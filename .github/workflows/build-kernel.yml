name: Build GKI Kernel - mayfly (KernelSU Next + SuSFS)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build GKI 5.10 + KSU Next + SuSFS
    runs-on: ubuntu-22.04
    env:
      ARCH: arm64
      GDRIVE_FILE_ID: 10mVhr4fihWPPSz0ws-pY-YF-kboaFYCC
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends git curl wget python3 python3-pip bc bison flex libssl-dev libelf-dev binutils-aarch64-linux-gnu cpio zip unzip make ninja-build lz4 zstd jq

      - name: Install repo tool
        run: |
          mkdir -p ~/.bin
          curl -fsSL "https://storage.googleapis.com/git-repo-downloads/repo" > ~/.bin/repo
          chmod a+x ~/.bin/repo
          echo "$HOME/.bin" >> $GITHUB_PATH

      - name: Download boot.img from Google Drive
        run: |
          pip install -q gdown
          gdown "https://drive.google.com/uc?id=${GDRIVE_FILE_ID}" -O boot.img
          ls -lh boot.img

      - name: Initialize GKI kernel repo
        run: |
          mkdir -p kernel && cd kernel
          git config --global user.email "build@github.com"
          git config --global user.name "GitHub Actions"
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android12-5.10 --depth=1 --no-clone-bundle
          repo sync -c -j$(nproc) --no-clone-bundle --no-tags

      - name: Integrate KernelSU Next
        run: |
          cd kernel/common
          curl -fsSL "https://raw.githubusercontent.com/KernelSU-Next/KernelSU-Next/next-susfs/kernel/setup.sh" | bash -s next-susfs
          echo "KernelSU Next patched"

      - name: Clone and apply SuSFS patches
        run: |
          cd kernel
          git clone --depth=1 -b gki-android12-5.10 https://gitlab.com/simonpunk/susfs4ksu.git susfs4ksu
          cd common
          for patch in ../susfs4ksu/kernel_patches/*.patch; do
            patch -p1 < "$patch" || echo "Patch failed: $patch"
          done
          echo "SuSFS patches applied"

      - name: Remove protected_exports
        run: |
          cd kernel/common
          sed -i '/^[[:space:]]*"protected_exports"/d' android/abi_gki_protected_exports_aarch64 2>/dev/null || true
          sed -i '/^[[:space:]]*"protected_exports"/d' android/abi_gki_protected_exports_x86_64 2>/dev/null || true

      - name: Apply kernel config
        run: |
          cd kernel/common
          echo "CONFIG_KSU=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_DEBUG=n" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_SPOOFY_UNAME=y" >> arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> arch/arm64/configs/gki_defconfig
          
          # Hợp thức hóa file cấu hình defconfig bằng savedefconfig của nhân
          make ARCH=arm64 O=out gki_defconfig
          make ARCH=arm64 O=out savedefconfig
          cp -f out/defconfig arch/arm64/configs/gki_defconfig
          rm -rf out
          echo "Cấu hình defconfig đã chuẩn hóa!"

      - name: Build kernel
        run: |
          cd kernel
          export KMI_SYMBOL_LIST_STRICT_MODE=0
          export SKIP_DEFCONFIG_VALIDATION=1
          
          # Vượt qua bộ lọc so khớp nghiêm ngặt danh sách KMI Symbol của Google
          if [ -f "build/abi/compare_to_symbol_list" ]; then
            echo "#!/bin/bash" > build/abi/compare_to_symbol_list
            echo "echo 'Bypassed GKI Symbol check successfully'" >> build/abi/compare_to_symbol_list
            echo "exit 0" >> build/abi/compare_to_symbol_list
          fi
          
          BUILD_CONFIG=common/build.config.gki.aarch64 LTO=thin SKIP_MRPROPER=0 build/build.sh 2>&1 | tee /tmp/build.log
        timeout-minutes: 90

      - name: Download magiskboot
        run: |
          # Giả lập User-Agent đầy đủ để vượt qua bộ chặn rate-limit của GitHub từ nhà mạng
          curl -A "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36" -L -s "https://github.com/topjohnwu/Magisk/releases/download/v27.0/Magisk-v27.0.apk" -o magisk.apk
          unzip -p magisk.apk lib/x86_64/libmagiskboot.so > magiskboot
          rm -f magisk.apk
          chmod +x magiskboot
          ls -lh magiskboot

      - name: Repack boot.img
        run: |
          # Khai báo đường dẫn cố định tuyệt đối tới file lõi Image vừa build ra (không dùng find mù quáng)
          TARGET_IMAGE="kernel/out/android12-5.10/common/arch/arm64/boot/Image"
          
          # Nếu build.sh xuất dạng nén lz4/gz, tự động cập nhật
          [ ! -f "$TARGET_IMAGE" ] && TARGET_IMAGE=$(find kernel/out -type f -name "Image" -o -name "Image.lz4" -o -name "Image.gz" | grep -v "\.o" | head -1)
          [ -z "$TARGET_IMAGE" ] && TARGET_IMAGE=$(find kernel -type f -name "Image" | grep -v "\.o" | grep "boot/Image" | head -1)
          
          if [ -z "$TARGET_IMAGE" ] || [ -d "$TARGET_IMAGE" ]; then
            echo "ERROR: Không tìm thấy file kernel lõi Image hợp lệ!"
            exit 1
          fi
          
          echo "-> Tìm thấy file Kernel Image chuẩn tại: $TARGET_IMAGE"
          mkdir -p repack && cd repack
          
          # Copy boot.img gốc từ Google Drive vào
          cp "${{ github.workspace }}/boot.img" ./boot.img
          
          # Bung boot.img gốc
          ../magiskboot unpack boot.img
          
          # Ghi đè file nhân mới vào (ép buộc xóa bỏ bản cũ nếu có)
          rm -f ./kernel
          cp -f "${{ github.workspace }}/$TARGET_IMAGE" ./kernel
          
          # Đóng gói lại thành phẩm
          ../magiskboot repack boot.img new-boot.img
          echo "-> Kích thước file boot.img thành phẩm sau repack:"
          ls -lh new-boot.img

      - name: Upload new-boot.img
        uses: actions/upload-artifact@v4
        with:
          name: mayfly-boot-ksu-susfs-${{ github.run_number }}
          path: repack/new-boot.img
          retention-days: 7

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ github.run_number }}
          path: /tmp/build.log
          retention-days: 3
